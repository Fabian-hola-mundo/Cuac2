# SEO Completo — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implementar SEO completo en cuacdesign.com: prerendering estático de rutas públicas, meta tags dinámicos por página, JSON-LD, robots.txt, sitemap.xml e imagen OG.

**Architecture:** Un `SeoService` centralizado inyectado en cada componente de ruta pública gestiona `<title>`, meta tags, canonical y JSON-LD. El prerendering SSG con `@angular/ssr` genera HTML estático en build time para las 8 rutas conocidas, que se despliegan en Firebase Hosting como archivos estáticos. Las rutas dinámicas `/portafolio/:slug` siguen siendo CSR pero con meta tags que Google indexa via JS rendering.

**Tech Stack:** Angular 21 (`@angular/ssr`, `Title`, `Meta`, `DOCUMENT`), Firebase Hosting, pnpm.

---

## Archivos

| Archivo | Acción |
|---|---|
| `src/app/core/services/seo.service.ts` | Crear |
| `public/robots.txt` | Crear |
| `public/sitemap.xml` | Crear |
| `public/assets/og-cuac.jpg` | Crear (manual) |
| `firebase.json` | Crear |
| `server.ts` | Generado por `ng add @angular/ssr` |
| `src/main.server.ts` | Generado por `ng add @angular/ssr` |
| `src/app/app.config.server.ts` | Generado por `ng add @angular/ssr` |
| `angular.json` | Modificar — agregar rutas de prerender |
| `src/app/app.config.ts` | Modificar — `provideClientHydration()` |
| `src/index.html` | Modificar — limpiar script dev, OG base |
| `src/app/pages/home/home.component.ts` | Modificar |
| `src/app/pages/cotizador/cotizador.component.ts` | Modificar |
| `src/app/pages/portafolio/portafolio-shell.component.ts` | Modificar |
| `src/app/pages/portafolio/portafolio-detail.component.ts` | Modificar |
| `src/app/pages/cuaquiverso/cuaquiverso.component.ts` | Modificar |
| `src/app/pages/cuaquiverso/tienda/tienda.component.ts` | Modificar |
| `src/app/pages/cuaquiverso/universo/universo.component.ts` | Modificar |
| `src/app/pages/identidadcorporativa/identidadcorporativa.component.ts` | Modificar |
| `src/app/pages/designsystem/designsystem.component.ts` | Modificar |

---

## Task 1: Instalar @angular/ssr y configurar prerendering

**Files:**
- Modify: `angular.json`
- Modify: `src/app/app.config.ts`
- Create (auto): `server.ts`, `src/main.server.ts`, `src/app/app.config.server.ts`

- [ ] **Step 1: Instalar @angular/ssr**

```bash
npx ng add @angular/ssr --skip-confirmation
```

Esperado: paquete instalado, archivos `server.ts`, `src/main.server.ts`, `src/app/app.config.server.ts` creados, `angular.json` y `app.config.ts` actualizados.

- [ ] **Step 2: Configurar las rutas de prerender en `angular.json`**

Dentro del bloque `"build" > "options"` agregar (si `ng add` no las agregó automáticamente, o reemplazar `"prerender": true` con):

```json
"server": "src/main.server.ts",
"prerender": {
  "routes": [
    "/",
    "/cotizar",
    "/portafolio",
    "/portafolio/natalia",
    "/portafolio/nathali",
    "/cuaquiverso",
    "/cuaquiverso/tienda",
    "/cuaquiverso/universo"
  ]
},
"ssr": {
  "entry": "server.ts"
}
```

- [ ] **Step 3: Verificar que `app.config.ts` tiene `provideClientHydration()`**

Si `ng add` no lo agregó, actualizar `src/app/app.config.ts`:

```typescript
import { ApplicationConfig, provideBrowserGlobalErrorListeners } from '@angular/core';
import { provideRouter, withViewTransitions } from '@angular/router';
import { provideClientHydration } from '@angular/platform-browser';
import { routes } from './app.routes';

export const appConfig: ApplicationConfig = {
  providers: [
    provideBrowserGlobalErrorListeners(),
    provideRouter(routes, withViewTransitions({ skipInitialTransition: true })),
    provideClientHydration(),
  ],
};
```

- [ ] **Step 4: Build de verificación**

```bash
npx ng build 2>&1 | tail -10
```

Esperado: `Application bundle generation complete.` y archivos HTML en `dist/cuac-design/browser/` (incluyendo `index.html`, `cotizar/index.html`, etc.).

- [ ] **Step 5: Verificar HTML prerendizado**

```bash
Get-Content "dist\cuac-design\browser\cotizar\index.html" | Select-String "<title>"
```

Esperado: el `<title>` con el contenido del `index.html` base (aún estático; los meta dinámicos se agregan en tareas posteriores).

- [ ] **Step 6: Commit**

```bash
git add angular.json src/app/app.config.ts server.ts src/main.server.ts src/app/app.config.server.ts
git commit -m "feat(seo): install @angular/ssr, configure SSG prerender routes"
```

---

## Task 2: Crear `SeoService`

**Files:**
- Create: `src/app/core/services/seo.service.ts`

- [ ] **Step 1: Crear el archivo del servicio**

```typescript
// src/app/core/services/seo.service.ts
import { Injectable, inject } from '@angular/core';
import { Title, Meta } from '@angular/platform-browser';
import { DOCUMENT } from '@angular/common';
import { PortfolioProject } from './portfolio.service';

const SITE_NAME = 'Cuac Design';
const BASE_URL  = 'https://cuacdesign.com';
const OG_IMAGE  = `${BASE_URL}/assets/og-cuac.jpg`;

export interface SeoConfig {
  title:       string;
  description: string;
  canonical:   string;
  ogImage?:    string;
  ogType?:     'website' | 'article';
  noindex?:    boolean;
}

@Injectable({ providedIn: 'root' })
export class SeoService {
  private titleSvc = inject(Title);
  private meta     = inject(Meta);
  private doc      = inject(DOCUMENT);

  set(config: SeoConfig): void {
    const fullTitle = config.title.includes(SITE_NAME)
      ? config.title
      : `${config.title} | ${SITE_NAME}`;
    const ogImg  = config.ogImage ?? OG_IMAGE;
    const ogType = config.ogType  ?? 'website';

    this.titleSvc.setTitle(fullTitle);

    this.meta.updateTag({ name: 'description',         content: config.description });
    this.meta.updateTag({ name: 'robots',              content: config.noindex ? 'noindex, nofollow' : 'index, follow' });
    this.meta.updateTag({ property: 'og:title',        content: fullTitle });
    this.meta.updateTag({ property: 'og:description',  content: config.description });
    this.meta.updateTag({ property: 'og:url',          content: config.canonical });
    this.meta.updateTag({ property: 'og:image',        content: ogImg });
    this.meta.updateTag({ property: 'og:type',         content: ogType });
    this.meta.updateTag({ name: 'twitter:card',        content: 'summary_large_image' });
    this.meta.updateTag({ name: 'twitter:title',       content: fullTitle });
    this.meta.updateTag({ name: 'twitter:description', content: config.description });
    this.meta.updateTag({ name: 'twitter:image',       content: ogImg });

    this.setCanonical(config.canonical);
  }

  setProject(project: PortfolioProject): void {
    const raw  = project.headline ?? project.description ?? '';
    const desc = raw.length > 155 ? raw.slice(0, 152) + '…' : raw;
    this.set({
      title:       project.title,
      description: desc,
      canonical:   `${BASE_URL}/portafolio/${project.slug}`,
      ogImage:     project.cover_url ?? OG_IMAGE,
      ogType:      'article',
    });
  }

  setJsonLd(schema: object): void {
    const id     = 'json-ld-schema';
    let   script = this.doc.getElementById(id) as HTMLScriptElement | null;
    if (!script) {
      script      = this.doc.createElement('script');
      script.id   = id;
      script.type = 'application/ld+json';
      this.doc.head.appendChild(script);
    }
    script.textContent = JSON.stringify(schema);
  }

  private setCanonical(url: string): void {
    let link = this.doc.querySelector<HTMLLinkElement>('link[rel="canonical"]');
    if (!link) {
      link = this.doc.createElement('link');
      link.setAttribute('rel', 'canonical');
      this.doc.head.appendChild(link);
    }
    link.setAttribute('href', url);
  }
}
```

- [ ] **Step 2: Build de verificación**

```bash
npx ng build 2>&1 | tail -5
```

Esperado: `Application bundle generation complete.` sin errores TypeScript.

- [ ] **Step 3: Commit**

```bash
git add src/app/core/services/seo.service.ts
git commit -m "feat(seo): add SeoService with set(), setProject(), setJsonLd()"
```

---

## Task 3: Archivos estáticos — robots.txt, sitemap.xml, firebase.json, og-cuac.jpg

**Files:**
- Create: `public/robots.txt`
- Create: `public/sitemap.xml`
- Create: `public/assets/og-cuac.jpg` (manual)
- Create: `firebase.json`

- [ ] **Step 1: Crear `public/robots.txt`**

```
User-agent: *
Allow: /

Disallow: /admin/
Disallow: /identidadcorporativa
Disallow: /designsystem

Sitemap: https://cuacdesign.com/sitemap.xml
```

- [ ] **Step 2: Crear `public/sitemap.xml`**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://cuacdesign.com/</loc>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>
  <url>
    <loc>https://cuacdesign.com/cotizar</loc>
    <changefreq>monthly</changefreq>
    <priority>0.9</priority>
  </url>
  <url>
    <loc>https://cuacdesign.com/portafolio</loc>
    <changefreq>weekly</changefreq>
    <priority>0.8</priority>
  </url>
  <url>
    <loc>https://cuacdesign.com/portafolio/natalia</loc>
    <changefreq>weekly</changefreq>
    <priority>0.7</priority>
  </url>
  <url>
    <loc>https://cuacdesign.com/portafolio/nathali</loc>
    <changefreq>weekly</changefreq>
    <priority>0.7</priority>
  </url>
  <url>
    <loc>https://cuacdesign.com/cuaquiverso</loc>
    <changefreq>monthly</changefreq>
    <priority>0.7</priority>
  </url>
  <url>
    <loc>https://cuacdesign.com/cuaquiverso/tienda</loc>
    <changefreq>weekly</changefreq>
    <priority>0.6</priority>
  </url>
  <url>
    <loc>https://cuacdesign.com/cuaquiverso/universo</loc>
    <changefreq>monthly</changefreq>
    <priority>0.6</priority>
  </url>
</urlset>
```

- [ ] **Step 3: Crear imagen OG placeholder**

Crear el directorio `public/assets/` si no existe. Agregar `public/assets/og-cuac.jpg`: imagen JPG de **1200×630px** con fondo Carbon `#151F28` y el logotipo de Cuac Design. Exportar desde Figma/Canva. Esta imagen aparece al compartir el sitio en redes sociales — debe reemplazarse con la imagen definitiva antes del lanzamiento.

Si se necesita un placeholder temporal para continuar el desarrollo, copiar cualquier JPG de 1200×630 y renombrarlo `og-cuac.jpg`.

- [ ] **Step 4: Crear `firebase.json`**

En la raíz del proyecto:

```json
{
  "hosting": {
    "public": "dist/cuac-design/browser",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ],
    "headers": [
      {
        "source": "**/*.@(js|css|jpg|jpeg|png|svg|gif|ico|woff|woff2|webp)",
        "headers": [
          {
            "key": "Cache-Control",
            "value": "public, max-age=31536000, immutable"
          }
        ]
      }
    ]
  }
}
```

- [ ] **Step 5: Commit**

```bash
git add public/robots.txt public/sitemap.xml public/assets/og-cuac.jpg firebase.json
git commit -m "feat(seo): add robots.txt, sitemap.xml, og image placeholder, firebase.json"
```

---

## Task 4: Limpiar `index.html` y completar OG base

**Files:**
- Modify: `src/index.html`

- [ ] **Step 1: Reemplazar el contenido completo de `src/index.html`**

```html
<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8">
  <title>Cuac Design — Estudio creativo. Branding, editorial e ilustración desde Bogotá.</title>
  <base href="/">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="description" content="Cuac Design es un estudio creativo colombiano especializado en branding, diseño editorial, ilustración y diseño web para fundadores, medianas y grandes empresas.">
  <meta name="robots" content="index, follow">
  <meta name="theme-color" content="#151F28">

  <!-- Open Graph -->
  <meta property="og:site_name" content="Cuac Design">
  <meta property="og:locale" content="es_CO">
  <meta property="og:type" content="website">
  <meta property="og:title" content="Cuac Design — Estudio creativo">
  <meta property="og:description" content="Branding, editorial, ilustración y diseño web desde Bogotá. Para marcas que quieren verse tan bien como son.">
  <meta property="og:url" content="https://cuacdesign.com">
  <meta property="og:image" content="https://cuacdesign.com/assets/og-cuac.jpg">

  <!-- Twitter / X -->
  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:site" content="@cuacdesign">
  <meta name="twitter:title" content="Cuac Design — Estudio creativo">
  <meta name="twitter:description" content="Branding, editorial, ilustración y diseño web desde Bogotá.">
  <meta name="twitter:image" content="https://cuacdesign.com/assets/og-cuac.jpg">

  <!-- Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@300;400;500;600;700;800&family=Fauna+One&display=swap" rel="stylesheet">
</head>
<body>
  <app-root></app-root>
</body>
</html>
```

- [ ] **Step 2: Build de verificación**

```bash
npx ng build 2>&1 | tail -5
```

Esperado: `Application bundle generation complete.` sin errores.

- [ ] **Step 3: Commit**

```bash
git add src/index.html
git commit -m "feat(seo): clean index.html — remove dev script, complete OG base meta"
```

---

## Task 5: SEO en HomeComponent + JSON-LD DesignCompany

**Files:**
- Modify: `src/app/pages/home/home.component.ts`

- [ ] **Step 1: Actualizar `home.component.ts`**

```typescript
import { Component, inject, OnInit } from '@angular/core';
import { HeroComponent } from './sections/hero/hero.component';
import { TickerComponent } from './sections/ticker/ticker.component';
import { ServicesComponent } from './sections/services/services.component';
import { TiersComponent } from './sections/tiers/tiers.component';
import { ProcessComponent } from './sections/process/process.component';
import { CaseStudyComponent } from './sections/case-study/case-study.component';
import { TestimonialsComponent } from './sections/testimonials/testimonials.component';
import { CtaComponent } from './sections/cta/cta.component';
import { SeoService } from '../../core/services/seo.service';

@Component({
  selector: 'app-home',
  standalone: true,
  imports: [
    HeroComponent,
    TickerComponent,
    ServicesComponent,
    TiersComponent,
    ProcessComponent,
    CaseStudyComponent,
    TestimonialsComponent,
    CtaComponent,
  ],
  templateUrl: './home.component.html',
  styleUrl: './home.component.scss',
})
export class HomeComponent implements OnInit {
  private seo = inject(SeoService);

  ngOnInit(): void {
    this.seo.set({
      title:       'Cuac Design — Estudio creativo',
      description: 'Branding, diseño editorial, ilustración y web desde Bogotá para marcas que quieren verse tan bien como son.',
      canonical:   'https://cuacdesign.com',
    });

    this.seo.setJsonLd({
      '@context':    'https://schema.org',
      '@type':       'DesignCompany',
      name:          'Cuac Design',
      description:   'Estudio creativo colombiano especializado en branding, diseño editorial, ilustración y diseño web.',
      url:           'https://cuacdesign.com',
      logo:          'https://cuacdesign.com/assets/og-cuac.jpg',
      foundingDate:  '2024',
      address: {
        '@type':          'PostalAddress',
        addressLocality:  'Bogotá',
        addressCountry:   'CO',
      },
      sameAs: [
        'https://www.instagram.com/cuac.design/',
        'https://www.linkedin.com/company/cuac-design/',
      ],
    });
  }
}
```

- [ ] **Step 2: Verificar que el import de `HomeComponent` coincide con el existente**

Leer el archivo actual antes de reemplazar para verificar que los imports de secciones son correctos. Si algún import difiere del listado arriba, ajustar.

- [ ] **Step 3: Build de verificación**

```bash
npx ng build 2>&1 | tail -5
```

Esperado: `Application bundle generation complete.` sin errores.

- [ ] **Step 4: Verificar HTML prerendizado del home**

```bash
Get-Content "dist\cuac-design\browser\index.html" | Select-String "title|description|DesignCompany" | Select-Object -First 10
```

Esperado: título correcto, meta description, y el JSON-LD en el HTML estático.

- [ ] **Step 5: Commit**

```bash
git add src/app/pages/home/home.component.ts
git commit -m "feat(seo): HomeComponent — meta tags + DesignCompany JSON-LD"
```

---

## Task 6: SEO en CotizadorComponent

**Files:**
- Modify: `src/app/pages/cotizador/cotizador.component.ts`

- [ ] **Step 1: Agregar SeoService al componente**

En `src/app/pages/cotizador/cotizador.component.ts`, agregar al inicio de la clase:

```typescript
private seo = inject(SeoService);
```

Agregar `SeoService` al import del archivo:

```typescript
import { SeoService } from '../../core/services/seo.service';
```

Agregar `OnInit` a la implementación de la clase si no existe:

```typescript
export class CotizadorComponent implements OnInit {
```

Agregar al final de `ngOnInit` (si ya existe) o crear el método:

```typescript
ngOnInit(): void {
  this.seo.set({
    title:       'Cotiza tu proyecto',
    description: 'Estima el costo de tu proyecto de diseño en segundos. Branding, web, editorial e ilustración desde Bogotá.',
    canonical:   'https://cuacdesign.com/cotizar',
  });
}
```

**Importante:** El `CotizadorComponent` ya tiene un `ngOnInit` que no existe en la clase (usa `@ViewChild` y `inject(FormBuilder)`). Revisar el archivo actual para no sobrescribir ninguna inicialización existente — agregar la llamada a `seo.set()` al inicio del `ngOnInit`.

- [ ] **Step 2: Build de verificación**

```bash
npx ng build 2>&1 | tail -5
```

Esperado: `Application bundle generation complete.` sin errores.

- [ ] **Step 3: Commit**

```bash
git add src/app/pages/cotizador/cotizador.component.ts
git commit -m "feat(seo): CotizadorComponent — meta tags"
```

---

## Task 7: SEO en PortafolioShellComponent (3 temas)

**Files:**
- Modify: `src/app/pages/portafolio/portafolio-shell.component.ts`

- [ ] **Step 1: Agregar SeoService e importarlo**

En `src/app/pages/portafolio/portafolio-shell.component.ts`:

Agregar import:
```typescript
import { SeoService } from '../../core/services/seo.service';
```

Inyectar en la clase:
```typescript
private seo = inject(SeoService);
```

- [ ] **Step 2: Agregar llamada SEO en `ngOnInit`**

El componente ya tiene `ngOnInit`. Agregar al inicio de ese método (antes o después de la carga existente):

```typescript
// SEO — se llama aquí porque el theme ya está seteado desde route.snapshot.data
this.applySeo();
```

Y agregar el método privado:

```typescript
private applySeo(): void {
  const t = this.theme();
  if (t === 'natalia') {
    this.seo.set({
      title:       'Portafolio — Natalia Castañeda',
      description: 'Diseño editorial, ilustración y branding. Portafolio personal de Natalia Castañeda Caicedo.',
      canonical:   'https://cuacdesign.com/portafolio/natalia',
    });
  } else if (t === 'nathali') {
    this.seo.set({
      title:       'Portafolio — Nathali Ramírez',
      description: 'Diseño UI/UX, ilustración y branding. Portafolio personal de Nathali Ramírez Ortiz.',
      canonical:   'https://cuacdesign.com/portafolio/nathali',
    });
  } else {
    this.seo.set({
      title:       'Portafolio',
      description: 'Proyectos de branding, diseño editorial, ilustración y web del estudio Cuac Design en Bogotá.',
      canonical:   'https://cuacdesign.com/portafolio',
    });
  }
}
```

- [ ] **Step 3: Build de verificación**

```bash
npx ng build 2>&1 | tail -5
```

Esperado: `Application bundle generation complete.` sin errores.

- [ ] **Step 4: Commit**

```bash
git add src/app/pages/portafolio/portafolio-shell.component.ts
git commit -m "feat(seo): PortafolioShellComponent — theme-aware meta tags"
```

---

## Task 8: SEO en PortafolioDetailComponent + JSON-LD CreativeWork

**Files:**
- Modify: `src/app/pages/portafolio/portafolio-detail.component.ts`

- [ ] **Step 1: Agregar SeoService**

En `src/app/pages/portafolio/portafolio-detail.component.ts`:

Agregar import:
```typescript
import { SeoService } from '../../core/services/seo.service';
```

Inyectar en la clase:
```typescript
private seo = inject(SeoService);
```

- [ ] **Step 2: Llamar a SEO y JSON-LD después de cargar el proyecto**

En el `ngOnInit`, inmediatamente después de `this.project.set(p)`:

```typescript
// Justo después de: this.project.set(p);
this.seo.setProject(p);
this.seo.setJsonLd({
  '@context':   'https://schema.org',
  '@type':      'CreativeWork',
  name:         p.title,
  description:  p.headline ?? p.description ?? '',
  creator:      { '@type': 'Organization', name: 'Cuac Design' },
  image:        p.cover_url ?? '',
  url:          `https://cuacdesign.com/portafolio/${p.slug}`,
});
```

- [ ] **Step 3: Build de verificación**

```bash
npx ng build 2>&1 | tail -5
```

Esperado: `Application bundle generation complete.` sin errores.

- [ ] **Step 4: Commit**

```bash
git add src/app/pages/portafolio/portafolio-detail.component.ts
git commit -m "feat(seo): PortafolioDetailComponent — dynamic meta + CreativeWork JSON-LD"
```

---

## Task 9: SEO en páginas de Cuaquiverso

**Files:**
- Modify: `src/app/pages/cuaquiverso/cuaquiverso.component.ts`
- Modify: `src/app/pages/cuaquiverso/tienda/tienda.component.ts`
- Modify: `src/app/pages/cuaquiverso/universo/universo.component.ts`

- [ ] **Step 1: Actualizar `cuaquiverso.component.ts`**

Agregar import:
```typescript
import { SeoService } from '../../core/services/seo.service';
```

Agregar a la clase `implements OnInit` si no lo tiene. Inyectar y llamar en `ngOnInit`:

```typescript
private seo = inject(SeoService);

ngOnInit(): void {
  this.seo.set({
    title:       'Cuaquiverso — División de producto de Cuac',
    description: 'Personajes colombianos traducidos a objetos: camisetas, libretas, stickers y más.',
    canonical:   'https://cuacdesign.com/cuaquiverso',
  });
  // resto del ngOnInit existente...
}
```

Si el componente ya tiene `ngOnInit`, agregar la llamada al inicio del método existente.

- [ ] **Step 2: Actualizar `tienda.component.ts`**

Agregar import:
```typescript
import { SeoService } from '../../../core/services/seo.service';
```

Agregar `implements OnInit`. Inyectar y llamar en `ngOnInit`:

```typescript
private seo = inject(SeoService);

ngOnInit(): void {
  this.seo.set({
    title:       'Tienda — Cuaquiverso',
    description: 'Compra productos del universo Cuaquiverso: camisetas, libretas, stickers y peluches.',
    canonical:   'https://cuacdesign.com/cuaquiverso/tienda',
  });
  // resto del ngOnInit existente...
}
```

- [ ] **Step 3: Actualizar `universo.component.ts`**

Agregar import:
```typescript
import { SeoService } from '../../../core/services/seo.service';
```

Agregar `implements OnInit`. Inyectar y llamar en `ngOnInit`:

```typescript
private seo = inject(SeoService);

ngOnInit(): void {
  this.seo.set({
    title:       'El universo — Cuaquiverso',
    description: 'Conoce los personajes del Cuaquiverso: Cuac, Kiki, Roar, Yeison y más.',
    canonical:   'https://cuacdesign.com/cuaquiverso/universo',
  });
  // resto del ngOnInit existente...
}
```

- [ ] **Step 4: Build de verificación**

```bash
npx ng build 2>&1 | tail -5
```

Esperado: `Application bundle generation complete.` sin errores.

- [ ] **Step 5: Commit**

```bash
git add src/app/pages/cuaquiverso/cuaquiverso.component.ts src/app/pages/cuaquiverso/tienda/tienda.component.ts src/app/pages/cuaquiverso/universo/universo.component.ts
git commit -m "feat(seo): Cuaquiverso pages — meta tags"
```

---

## Task 10: Noindex en IdentidadCorporativa y DesignSystem

**Files:**
- Modify: `src/app/pages/identidadcorporativa/identidadcorporativa.component.ts`
- Modify: `src/app/pages/designsystem/designsystem.component.ts`

- [ ] **Step 1: Actualizar `identidadcorporativa.component.ts`**

Agregar import:
```typescript
import { SeoService } from '../../core/services/seo.service';
```

Agregar `implements OnInit` a la clase. Inyectar y llamar:

```typescript
private seo = inject(SeoService);

ngOnInit(): void {
  this.seo.set({
    title:       'Brandbook',
    description: 'Manual de marca de Cuac Design.',
    canonical:   'https://cuacdesign.com/identidadcorporativa',
    noindex:     true,
  });
  // resto del ngOnInit existente si lo hay...
}
```

- [ ] **Step 2: Actualizar `designsystem.component.ts`**

Agregar import:
```typescript
import { SeoService } from '../../core/services/seo.service';
```

Agregar `implements OnInit`. Inyectar y llamar:

```typescript
private seo = inject(SeoService);

ngOnInit(): void {
  this.seo.set({
    title:       'Design System',
    description: 'Sistema de diseño de Cuac Design.',
    canonical:   'https://cuacdesign.com/designsystem',
    noindex:     true,
  });
  // resto del ngOnInit existente si lo hay...
}
```

- [ ] **Step 3: Build de verificación**

```bash
npx ng build 2>&1 | tail -5
```

Esperado: `Application bundle generation complete.` sin errores.

- [ ] **Step 4: Commit**

```bash
git add src/app/pages/identidadcorporativa/identidadcorporativa.component.ts src/app/pages/designsystem/designsystem.component.ts
git commit -m "feat(seo): noindex on brandbook and design system pages"
```

---

## Task 11: Verificación final del build y HTML prerendizado

**Files:** Ninguno (solo verificación)

- [ ] **Step 1: Build de producción completo**

```bash
npx ng build 2>&1 | tail -15
```

Esperado: `Application bundle generation complete.` Verificar que aparecen las 8 rutas prerendizadas en el output.

- [ ] **Step 2: Verificar meta tags en el HTML del home**

```bash
Get-Content "dist\cuac-design\browser\index.html" | Select-String "title|description|og:|twitter:|canonical|json-ld|DesignCompany"
```

Esperado: todos los meta tags presentes en el HTML estático.

- [ ] **Step 3: Verificar meta tags en cotizar**

```bash
Get-Content "dist\cuac-design\browser\cotizar\index.html" | Select-String "title|description|canonical"
```

Esperado: `<title>Cotiza tu proyecto | Cuac Design</title>` y description correcta.

- [ ] **Step 4: Verificar robots.txt y sitemap en dist**

```bash
Test-Path "dist\cuac-design\browser\robots.txt"
Test-Path "dist\cuac-design\browser\sitemap.xml"
```

Esperado: `True` para ambos.

- [ ] **Step 5: Verificar que el script dev no está en el HTML**

```bash
Get-Content "dist\cuac-design\browser\index.html" | Select-String "localhost:8400"
```

Esperado: sin resultados (vacío).

- [ ] **Step 6: Commit final**

```bash
git add -A
git commit -m "feat(seo): verified — all meta tags, prerendering, robots, sitemap complete"
```
